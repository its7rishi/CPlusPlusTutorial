#include <iostream>
#include <cmath> // Math functions like sqrt, round etc

using namespace std;

int main() {

    // max => Find the highest value
    // cout << max(5,19);

    // min => Find the lowest value
    // cout << min(19,55);

    // sqrt => Square Root
    // cout << "Square root of 64 is: " << sqrt(64) << endl;

    // round => Rounds a number
    // cout << "The rounded value of 4.8 is: " << round(4.8) << endl;

    // log => gives the logarithmic value
    // cout << "The logarithmic value of 2 is: " << log(2) << endl;

    // ceil => rounds up a number to the nearest integer
    // cout << "The rounded up value of 4.3 is " << ceil(4.3) << endl;

    // floor => rounds down a number to the nearest integer
    cout << "The rounded down value of 7.8 is: " << floor(7.8) << endl;
    return 0;
}
